[DEFAULT]
# git-buildpackage layout: upstream lives on `main`, packaging on
# `debian/main`, debian-tagged commits as `debian/<version>`.
debian-branch = debian/main
upstream-branch = main
upstream-tag = v%(version)s
debian-tag = debian/%(version)s
pristine-tar = False

[buildpackage]
sign-tags = True
